module encode::homework{

    // use std::signer;
    // use std::string;
    // use std::u64;

    struct Exercises has key, store{
        
        value: u64
    }

    fun init_module(account: &signer){

    }

}